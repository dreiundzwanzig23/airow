rgr:red
- Added failing batch-focused unit, integration, and system tests covering
  ordered case overrides, per-case failure isolation, CLI dispatch, and batch
  summary emission for `R-025`.

rgr:green
- Implemented `BatchSimulationConfig` and `BatchSimulationResult`, file-backed
  batch definition loading, ordered case execution through `run_simulation`,
  batch CLI dispatch, and deterministic batch summary JSON emission.

rgr:refactor
- No-op structural refactor: the batch path already reuses the existing
  single-run orchestration and output seams directly, so no further
  behavior-preserving cleanup was needed beyond isolated output-path handling.
