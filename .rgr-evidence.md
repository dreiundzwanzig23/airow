rgr:red
- Added failing provider-selection, built-in runtime-construction, structured
  provider-metadata, and headless provider-validity tests for Slice 1.
- Added failing hydro-fidelity tests for low-speed-damped built-in hull drag
  and phase-shaped built-in blade propulsion before changing provider code.
- Added failing aero-fidelity tests for low-apparent-wind headwind drag
  sensitivity plus explicit lateral crosswind force and speed-shaped yaw
  before changing the steady-wind provider.

rgr:green
- Implemented config-driven built-in provider selection, structured provider
  metadata, updated example configs, and supporting runtime or output changes.
- Implemented in-place hydro-fidelity refinements for the existing built-in
  hull-resistance and blade-force ids and refreshed the affected scenario
  envelopes.
- Implemented in-place steady-wind aero-fidelity refinements for the existing
  built-in aero id and refreshed the affected wind-backed scenario envelopes.

rgr:refactor
- Split hydro runtime composition into restoring, hull-resistance, and
  blade-force roles while preserving the injected test seams and shared run
  path.
- No further refactor beyond the new seam-local hydro helper split was needed;
  the slice remained localized to `A-004` plus scenario evidence updates.
- No further refactor beyond seam-local aero helper extraction was needed; the
  slice remained localized to `A-005` plus wind-scenario evidence updates.
