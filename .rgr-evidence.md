# RGR Evidence

- `rgr:red`: Added `UT-071..UT-074`, `IT-009`, and `QT-012..QT-013` plus the
  checked-in `calm_water_stroke` scenario artifact, then confirmed the build
  failed because the `A-004` hydro runtime slice and structured blade-load
  contract did not exist yet.
- `rgr:green`: Implemented structured hydro load sampling, deterministic
  passive/tow/calm-water baseline providers, scenario-harness support for the
  calm-water scenario, and real blade-load propagation through results and
  outputs until the new targeted tests passed.
- `rgr:refactor`: No additional production-code cleanup was required after the
  green pass; the only post-green adjustment was tightening the new
  integration assertion so zero-speed drive samples do not falsely require
  positive instantaneous power.
- `rgr:red`: Added the `examples/` catalog plan as executable surface tests
  first by introducing `QT-014`/`QT-015`, which failed until the helper
  script, tracked example configs, and docs existed.
- `rgr:green`: Added the example configs, helper script, ignore coverage, and
  release-facing instructions until both the helper and direct CLI example
  paths ran successfully.
- `rgr:refactor`: Kept the example implementation minimal by reusing the
  existing CLI contract and a single dedicated system test file instead of
  scattering example checks across unrelated output tests.
