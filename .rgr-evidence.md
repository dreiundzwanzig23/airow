rgr:red: updated the default-path config, unit, and scenario expectations to
require `sundials_ida`, then reproduced failing red builds because the repo
had no SUNDIALS catalog entry, no default-path wiring, and no required build
dependency for the new backend.

rgr:green: added required SUNDIALS build wiring, the new `D-043`
SUNDIALS-backed state advancer, default `sundials_ida` config/metadata
behavior, and passing targeted unit, integration, and scenario evidence
including `UT-140`, `UT-141`, `QT-033`, and `QT-034`.

rgr:refactor: extracted the shared segmented advancement loop so deterministic
baseline, SUNDIALS, and Chrono reuse the same phase-boundary stepping and
post-step validation while backend-specific hull solvers stay localized.
