## RGR Evidence

Target:
- Requirement: R-050, R-052, R-053
- Architecture: A-007, A-008
- Design: D-030, D-063
- Test: QT-050, QT-051

rgr:red:
- Command: `ctest --test-dir build -R RunAnalysisSystem.PythonToolGeneratesInteractiveVisualizationReport --output-on-failure`
- Expected failing test: `RunAnalysisSystem.PythonToolGeneratesInteractiveVisualizationReport`
- Observed failure: report tool exited 2 because `--visualization` was not a supported argument, and no `index.html` was generated.
- Why this proves the intended missing behavior: the user workflow requires the offline report tool to consume the emitted visualization artifact and generate an interactive inspection page, which the existing static report path could not do.

rgr:green:
- Command: `ctest --test-dir build -R "RunAnalysisSystem.PythonTool(GeneratesInteractiveVisualizationReport|RejectsMalformedVisualizationArtifact)" --output-on-failure`
- Minimal implementation change: added optional visualization artifact loading and validation to `tools/run_analysis.py`, embedded viewer data in generated HTML, rendered 2D projection canvases, playback/scrub controls, selected vector labels, trust and unavailable-channel labels, and linked plot cursors.
- Observed pass: both focused system tests passed after implementation.

rgr:refactor:
- Cleanup performed: kept the viewer dependency-free inside the existing offline report generator, reused the visualization artifact validator instead of duplicating schema checks, tightened generated report styling, and regenerated the checked-in calm-water report bundle.
- Or explicit no-op rationale: not applicable.

Gates:
- test_tdd: `./scripts/test_tdd.sh` passed.
- format: `./scripts/format.sh` passed.
- lint: `./scripts/lint.sh` passed.
- build: `./scripts/build.sh` passed during focused iteration.
- test: `./scripts/test.sh` passed.
- depcheck: `./scripts/depcheck.sh` passed.
- tracecheck: `python3 tools/tracecheck.py --write` passed with existing numbering-gap warnings only.

## Simulation Evidence Design

Physical or interpretive claim:
- Users can inspect a reduced-runtime run's state evolution, frame directions, selected world-frame vectors, and time-series relationships without changing the numerical result.

Fidelity level:
- reduced visualization of the current reduced runtime.

Validity range:
- speed: inherited from the active provider trust envelope.
- stroke rate: inherited from the emitted scenario/configuration.
- wind/current/wave assumptions: visualized only when emitted by current providers; waves/current remain unavailable unless future providers add them.
- geometry assumptions: single-scull reduced hull/oar/blade reference geometry from the artifact, not imported production surfaces.
- solver/timestep assumptions: sample policy matches the emitted time-series artifact.

Oracle:
- visual invariant and deterministic artifact validation.
- details: malformed artifacts are rejected, vectors retain units and frames, unavailable channels remain labeled unavailable, and linked plots share the same sample index as the playback controls.

Required outputs:
- time-series channels: existing emitted records plus visualization samples.
- units: inherited from scalar and vector channel metadata.
- frames: world, hull-body, waterline, and port/starboard conventions.
- visualization vectors: hull hydro, blade, aero, wind, and rower inertial vectors when present.
- diagnostics: invalid visualization artifacts fail report generation with a deterministic error.

Tests:
- UT: existing output helper coverage for artifact/time-series shape.
- IT: existing output integration coverage for visualization metadata and sample alignment.
- QT: QT-049, QT-050, QT-051.

Failure modes:
- rejected: unsupported or malformed visualization schema.
- low-confidence: trust-envelope labels remain visible in the report.
- visually inspectable: unavailable channels are listed rather than drawn as implied zero or supported vectors.

## Test Lane Selection

Iteration lane:
- command: `./scripts/test_tdd.sh`
- why sufficient for current loop: it runs the fast evidence lane and strict RGR marker check after the focused system-test iteration.

Completion gates:
- commands: `./scripts/format.sh`, `./scripts/lint.sh`, `./scripts/build.sh`, `./scripts/test.sh`, `./scripts/depcheck.sh`, `python3 tools/tracecheck.py --write`
- status: passed

Optional broad confidence lane:
- omitted because the required full local gates cover this additive report-generator slice.

## Release Doc Sync

Trigger:
- user-visible

Updated:
- README: interactive report command and output description.
- CHANGELOG: public interactive inspection report behavior.
- docs/ai/SESSION_CONTEXT: current visualization path and next actions.
- docs/ai/HANDOFF: latest-session delta and carry-forward flags.
- docs/ai/ROADMAP: near-term visualization direction.
- docs/ai/DECISIONS: not updated.

Omitted docs:
- docs/ai/DECISIONS.md: no durable technology or policy decision changed.

## Trace Maintenance

Mode:
- repair

Changed links:
- R-050/R-052/R-053: added interactive report and malformed-artifact evidence.
- A-007/A-008: allocation updated for offline interactive report generation and evidence.
- D-030/D-063: reused existing report and visualization artifact designs.
- QT-050/QT-051: added.

Tracecheck:
- command: `python3 tools/tracecheck.py --write`
- result: Trace OK with pre-existing numbering-gap warnings.

Generated trace diff reviewed:
- yes
