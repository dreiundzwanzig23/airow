## RGR Evidence

Target:
- Requirement: R-023, R-024
- Architecture: A-001, A-002
- Design: D-001, D-040, D-047
- Test: UT-133, UT-246, UT-367

rgr:red
- Command: `ctest --test-dir build --output-on-failure -R "SimulatorConfigRuntime.RejectsLegacyStateAdvancerSelection|SimulatorConfigWind.RejectsMixedConstantAndSampledWindInputs|SimulatorConfigWind.RejectsRemovedConstantWindInput"`
- Expected failing test: UT-133, UT-246, UT-367
- Observed failure: UT-133 still reports `invalid_value` for
  `$.simulation.state_advancer`; UT-246 still reports mixed wind modes at
  `$.environment`; UT-367 still accepts `environment.ambient_wind_world_mps`.
- Why this proves the intended missing behavior: the current parser still
  preserves legacy-specific handling for `simulation.state_advancer` and still
  accepts `environment.ambient_wind_world_mps`.

rgr:green
- Command: `ctest --test-dir build --output-on-failure -R "SimulatorConfigRuntime.RejectsLegacyStateAdvancerSelection|SimulatorConfigWind.RejectsMixedConstantAndSampledWindInputs|SimulatorConfigWind.RejectsRemovedConstantWindInput"`
- Minimal implementation change: reject `simulation.state_advancer` and
  `environment.ambient_wind_world_mps` as unsupported fields, and stop
  emitting default constant-wind normalized metadata.
- Observed pass: all three targeted tests pass after rebuilding
  `project_tests_unit`.

rgr:refactor
- Cleanup performed: removed the config-level constant-wind C++ field after
  migrating callers to sampled wind inputs, and centralized propulsion-metric
  provider support in the provider catalog instead of duplicating provider-id
  checks in output and analysis code.

Gates:
- test_tdd: PASS (`./scripts/test_tdd.sh`)
- format: PASS (`./scripts/format.sh`)
- lint: PASS (`./scripts/lint.sh`)
- build: PASS (`./scripts/build.sh`)
- test: PASS (`./scripts/test.sh`)
- depcheck: PASS (`./scripts/depcheck.sh`)
- tracecheck: PASS with existing numbering-gap warnings
  (`python3 tools/tracecheck.py --write`)
