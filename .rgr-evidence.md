rgr:red: reproduced the failing strict full gate after runtime GoogleTest
discovery moved to `PRE_TEST`: sanitized and GCC CTest discovery ran without
the leak-suppression environment used by test execution, and the SUNDIALS
tests still depended on the ambient `AIROW_SUNDIALS_TEST_FAULT` runtime seam.

rgr:green: mirrored the required sanitizer environment into the sanitized and
GCC CTest presets, added tooling-contract coverage for those preset-level
requirements, and replaced the ambient SUNDIALS fault env with an explicit
test-only hook while keeping the targeted SUNDIALS evidence green.

rgr:refactor: centralized SUNDIALS fault injection behind
`PROJECT_TEST_HOOKS`, removed production runtime dependence on undocumented
environment variables, and synchronized workflow comments, README context, and
AI handoff/state docs to the freshly verified gate truth.
