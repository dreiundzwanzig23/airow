## RGR Evidence

Target:
- Requirement: R-050, R-052, R-053
- Architecture: A-007, A-008
- Design: D-063
- Test: UT-382, QT-052

rgr:red:
- Command: `ctest --test-dir build --output-on-failure -R "RunOutputs.VisualizationArtifactEmitsHullBodyFrameVectorChannels|RunAnalysisSystem.PythonToolReportsConfigurableVisualizationControls"`
- Expected failing tests: `RunOutputs.VisualizationArtifactEmitsHullBodyFrameVectorChannels` and `RunAnalysisSystem.PythonToolReportsConfigurableVisualizationControls`
- Observed failure: the artifact lacked `hull_hydro_moment_body` / `*_body_*` vector channels, and the offline report lacked body-frame vector toggle metadata.
- Why this proves the intended missing behavior: the previous visualization path exposed world-frame vectors and frame labels, but no transformed hull-body vector data for report controls to select.

rgr:green:
- Command: `ctest --test-dir build --output-on-failure -R "RunOutputs.VisualizationArtifactEmitsHullBodyFrameVectorChannels|RunAnalysisSystem.PythonToolReportsConfigurableVisualizationControls"`
- Minimal implementation change: `src/lib/output/run_output.cpp` now derives hull-body-frame vector variants from emitted world-frame vectors using the sample hull orientation, and `tools/run_analysis.py` maps those channels to provenance metadata for report controls.
- Observed pass: targeted unit and system tests passed after the additive serializer/report changes.

rgr:refactor:
- Cleanup performed: kept projection helpers local to output serialization, preserved the `airow.visualization.v1` additive schema boundary, and left unavailable interface/disturbance channels explicitly unavailable.

Gates:
- test_tdd: `./scripts/test_tdd.sh` passed after splitting the new unit test
  into `tests/unit/test_run_outputs_visualization.cpp` to satisfy auxiliary
  test-quality limits.
- format: `./scripts/format.sh` passed.
- lint: `./scripts/lint.sh` passed.
- build: `./scripts/build.sh` passed.
- test: `./scripts/test.sh` passed.
- depcheck: `./scripts/depcheck.sh` passed.
- tracecheck: `python3 tools/tracecheck.py --write` passed with existing
  numbering-gap warnings only.

## Simulation Evidence Design

Physical or interpretive claim:
- Users can inspect available reduced-runtime vectors in both world and
  hull-body frames without changing numerical results or implying support for
  unavailable interface/disturbance loads.

Fidelity level:
- reduced visualization.

Validity range:
- speed: inherited from emitted provider trust metadata.
- stroke rate: inherited from emitted scenario/configuration.
- wind/current/wave assumptions: transformed only for already-emitted vector
  channels; unavailable disturbances remain unavailable.
- geometry assumptions: single-scull reduced hull orientation from
  `airow.visualization.v1`.
- solver/timestep assumptions: body-frame vectors are derived per emitted
  visualization sample.

Oracle:
- visual invariant.
- details: inverse hull orientation transforms world vectors into hull-body
  coordinates; tests use a non-identity orientation and exact unit/provenance
  metadata checks.

Required outputs:
- time-series channels: unchanged.
- units: inherited from emitted vector channels.
- frames: `world` and `hull_body`.
- visualization vectors: hull hydro, blade, aero, ambient/apparent wind, and
  rower inertial vectors when emitted.
- diagnostics: unavailable interface/disturbance channels remain disabled.

Tests:
- UT: `UT-382`.
- IT: existing D-063 output integration.
- QT: `QT-052`.

Failure modes:
- rejected: malformed visualization artifacts remain rejected by the validator.
- low-confidence: trust-envelope labels remain visible in reports.
- visually inspectable: unsupported channels are disabled instead of drawn as
  zero vectors.

## Trace Maintenance

Mode:
- promotion

Changed links:
- R-050/R-052/R-053: existing QT-052 report-control evidence now checks
  hull-body vector controls.
- D-063: `UT-382` verifies additive body-frame visualization vector channels.

Tracecheck:
- command: `python3 tools/tracecheck.py --write`
- result: Trace OK, with pre-existing numbering-gap warnings only.

Generated trace diff reviewed:
- yes.

## Release Doc Sync

Trigger:
- user-visible, roadmap, architecture

Updated:
- README: offline report controls now mention world-frame and hull-body vector
  toggles.
- CHANGELOG: body-frame visualization vector channels.
- docs/ai/SESSION_CONTEXT: current visualization posture and remaining gaps.
- docs/ai/HANDOFF: latest-session delta and next steps.
- docs/ai/ROADMAP: near-term visualization follow-up after body-frame channels.
- docs/ai/DECISIONS: omitted, no durable technology decision changed.

## RGR Evidence

Target:
- Requirement: R-052, R-053, R-070
- Architecture: A-007, A-008
- Design: D-063
- Test: QT-053, QT-054

rgr:red:
- Command: `./scripts/test_tdd.sh`
- Expected failing tests: `RunAnalysisSystem.PythonToolReportsInteractiveEventMarkers` and `RunAnalysisSystem.PythonToolReportsMirroredMomentVectorCoverage`
- Observed failure: the new red assertions targeted missing `interactive_controls.event_markers`, missing HTML event seek controls, and vector controls that fell back to generic emitted-sample provenance for emitted moment vectors. The first full fast-lane attempt also exposed a changed-test helper structure-lint issue, which was corrected before green.
- Why this proves the intended missing behavior: the prior offline report could scrub and plot samples, but it did not expose derived peak/zero/stroke/trust event markers or provider provenance for all already-emitted force and moment vectors.

rgr:green:
- Command: `./scripts/test_tdd.sh`
- Minimal implementation change: `tools/run_analysis.py` now derives peak, zero-crossing, stroke-boundary, and trust-warning markers from existing summary/time-series data, renders HTML event seek controls, and writes those markers into `metrics.json.interactive_controls`; `src/lib/output/run_output.cpp` now declares metadata for already-emitted moment, apparent-wind, and rower-inertial visualization vectors.
- Observed pass: changed unit, integration, system, auxiliary, coverage, and coverage-ratchet lanes passed after repairing the pre-existing `karpathy-guidelines` skill index issue.

rgr:refactor:
- Cleanup performed: kept event marker shaping in small helpers, reused the existing sample-index timeline for marker seeking, and preserved the additive `airow.visualization.v1` schema boundary.

Gates:
- test_tdd: `./scripts/test_tdd.sh` passed.
- format: `./scripts/format.sh` passed.
- lint: `./scripts/lint.sh` passed.
- build: `./scripts/build.sh` passed.
- test: `./scripts/test.sh` passed.
- depcheck: `./scripts/depcheck.sh` passed.
- tracecheck: `python3 tools/tracecheck.py --write` passed with existing numbering-gap warnings only.

## Unit Test Design

Target:
- Design: D-014
- Test: UT-381

Cases:
- nominal: successful CLI run with optional visualization and truth-model artifact paths, plus HDF5 path when supported by the build.
- boundary: invalid `--config` and `--report` argument forms with missing or empty values.
- invalid/error: usage errors return exit code 64 and print usage.

Oracle:
- exact.
- expected observable: stdout includes emitted optional artifact path labels on success; invalid argument forms print usage and return 64.

Red expectation:
- This is coverage-ratchet remediation for existing D-014 behavior; the behavior already existed, but the branch evidence was missing.

## Trace Maintenance

Mode:
- promotion

Changed links:
- R-019: review flag cleared after QT-025 and trace JSON check.
- R-023: review flag cleared after QT-039 wind evidence check.
- R-052/R-053/R-070: QT-052 added for offline visualization report controls.
- R-052/R-053/R-070: QT-053 and QT-054 added for offline event markers and mirrored moment-vector evidence.
- D-014: UT-381 added for optional CLI artifact-path reporting.
- D-063: QT-052 exercises visualization report controls derived from emitted artifacts.
- Repository skill metadata: `karpathy-guidelines` now has the expected
  `SKILL.md` entry and load-on-demand indexes for auxiliary lint.

Tracecheck:
- command: `python3 tools/tracecheck.py --write`
- result: Trace OK, with pre-existing numbering-gap warnings only.

Generated trace diff reviewed:
- yes.

## Release Doc Sync

Trigger:
- user-visible, roadmap, architecture

Updated:
- README: offline report controls, event markers, and `metrics.json.interactive_controls`.
- CHANGELOG: visualization controls, event markers, and mirrored moment-vector evidence.
- docs/ai/SESSION_CONTEXT: current visualization posture and next actions.
- docs/ai/HANDOFF: latest-session delta.
- docs/ai/ROADMAP: near-term visualization follow-up.
- AGENTS.md and `.agents/skills/README.md`: indexed the existing
  `karpathy-guidelines` skill so auxiliary lint can validate it.
- docs/ai/DECISIONS: omitted, no durable technology decision changed.

Omitted docs:
- AGENTS.md: no workflow policy changed.

## Simulation Evidence Design

Physical or interpretive claim:
- Users can inspect reduced-runtime report controls for emitted vectors, frames, projections, plot channels, and derived event markers without changing numerical results.

Fidelity level:
- reduced visualization.

Validity range:
- speed: inherited from the emitted trust envelope.
- stroke rate: inherited from the scenario/configuration.
- wind/current/wave assumptions: only emitted channels are selectable; unavailable channels remain disabled.
- geometry assumptions: reduced single-scull reference transforms from `airow.visualization.v1`.
- solver/timestep assumptions: report controls use emitted sample indices and time-series records.

Oracle:
- visual invariant.
- details: controls and event markers are derived from artifact metadata or time-series samples, selected vectors retain unit/frame/provenance labels, unavailable channels are disabled, and plot or event clicks seek the shared timeline index.

Required outputs:
- time-series channels: generated plot-channel and event-marker metadata in `metrics.json`.
- units: vector units from visualization samples and plot units from emitted records.
- frames: world, hull-body, and waterline frame controls.
- visualization vectors: emitted vector channels plus disabled unavailable channels.
- diagnostics: unsupported or unavailable channels stay labeled rather than implied; trust warnings become explicit event markers.

Tests:
- UT: existing D-063 output-shape tests.
- IT: existing output integration coverage.
- QT: `QT-052`, `QT-053`, `QT-054`.

Failure modes:
- rejected: malformed visualization artifacts still fail before report generation.
- low-confidence: trust labels remain visible.
- visually inspectable: missing channels are disabled in controls and listed in metadata.
