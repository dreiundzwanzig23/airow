# AGENTS.md — tests/system

## Scope
Requirement-level qualification and acceptance tests.

## Rules
- `QT-*` IDs are mandatory.
- `@verifies` must reference `R-*` IDs.
- Record quantitative tolerances and scenario intent in `@notes`.

## Guardrails
- Keep scenarios deterministic and CI-feasible.
